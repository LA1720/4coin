{% extends 'users.html.twig' %}

{% block title %}Modification du profile de
	{{ app.user.firstname }}!
{% endblock %}

{% block body %}
	<div class="grix xs4 container">
		<div class="col-xs4">
			<h1>
				Modification du profile de
				{{ app.user.name }}
				{{ app.user.firstname }}
			</h1>
				{{ form(form) }}
		</div>
	</div>
{% endblock %}
